The impact assessment is above — the key blocker before implementation is clarifying which interpretation is intended: **A** (add `@nestjs/config` + tests for env-var loading) or **B** (Linear.app integration loading credentials from env). Both are low-to-medium scope backend changes with no frontend or shared-types impact under interpretation A, but B adds an external SDK and a new module.