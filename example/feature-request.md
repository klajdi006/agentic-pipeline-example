# Feature request

> Source: product@taskapp — submitted to the pipeline

We want a **deadline-based task scheduler**. Users should be able to set a deadline on a
task and get a reminder before it's due. Tasks with deadlines should be visible in a
"Due soon" view, sorted by deadline. Reminders should fire reliably without us standing
up new infrastructure if we can avoid it.
