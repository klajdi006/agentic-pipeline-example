The proposed standards update is in the plan file and shown above — a single Definition-of-done line requiring lockfile regeneration on any `package.json` version bump, backed by the major review finding. The two info-level findings are environment limitations, not rules, so no further changes are proposed.